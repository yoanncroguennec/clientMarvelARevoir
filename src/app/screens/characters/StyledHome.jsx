import { Box, styled } from "@mui/material";

export const RootHome = styled(Box)(({ theme }) => ({
    alignItems: "center",
    display: "flex",
    justifyContent: "center",
    flexWrap: "wrap",
    marginTop: "80px",
    width: "100vw"
}));
